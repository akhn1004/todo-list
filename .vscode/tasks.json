{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "webpack build",
            "command": ["${workspaceFolder}/node_modules/.bin/webpack -w"],
            "type": "shell",
            "problemMatcher": ["$tsc"]
        }
    ]
}
